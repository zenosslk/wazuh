<agent_config>

  <!-- Shared agent configuration here -->
    <wodle name="command">
      <tag>fluentbit</tag>
      <command>${DIRECTORY}/fluent-bit/fluent-bit -c ${DIRECTORY}/fluent-bit/fluent-bit-syslog.conf</command>
      <ignore_output>yes</ignore_output>
    </wodle>
</agent_config>
